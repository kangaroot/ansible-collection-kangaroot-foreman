---
# tasks file for foreman_content_foreman

- name: "Vars | Include Foreman content vars"
  ansible.builtin.include_vars:
    dir: vars
    ignore_files:
      - "main.yml"
    extensions:
      - "yml"
    ignore_unknown_extensions: true

