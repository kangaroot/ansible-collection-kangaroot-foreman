---
# tasks file for foreman_content_nginx

- name: "Vars | Include Nginx content vars"
  ansible.builtin.include_vars:
    dir: vars
    ignore_files:
      - "main.yml"
    extensions:
      - "yml"
    ignore_unknown_extensions: true

